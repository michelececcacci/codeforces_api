{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${file}",
            "showLog": true,
            "envFile": "${workspaceFolder}/.env" // here .env file must declare variables as local
        }
    ]
}